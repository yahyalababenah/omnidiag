# تقرير تدقيق وحدة السكري — OmniDiag

**التاريخ:** 17 سبتمبر 2026
**النطاق:** بيانات السكري، منهجية العتبة، ومعايرة الاحتمالات
**الأدلة:** `diabetes_audit.txt`, `prevalence_experiment_results.txt`, `prevalence_experiment_raw.csv`, `prevalence_experiment.png`
**الكود القابل لإعادة التشغيل:** `audit_diabetes.py`, `prevalence_experiment.py`

---

## الخلاصة التنفيذية

| البند | الحالة |
|---|---|
| اتجاه التسمية | ✅ سليم |
| القيم المفقودة | ✅ لا يوجد |
| التكرارات عبر التقسيم | ⚠️ 3.99%، أثرها 0.005 AUC |
| قدرة النموذج (ROC-AUC) | ✅ 0.8302 — صادقة |
| منهجية استخراج العتبة | ❌ من مجموعة الاختبار — أثرها 0.7% |
| **معايرة الاحتمالات** | ❌ **PPV ينهار من 67% إلى 25%** |
| تحقق خارجي | ❌ لا يوجد |

**الحكم:** النموذج سليم. المعطوب هو **نقطة التشغيل ومقياس الاحتمالات**، لا المعمارية ولا البيانات.

هذا يختلف جوهرياً عن وحدة القلب، حيث كانت البيانات نفسها مكسورة.

---

## 1. البيانات — سليمة

**الملف:** `diabetes_binary_5050split_health_indicators_BRFSS2015.csv`
**الأبعاد:** 70,692 صفاً × 22 عموداً (21 ميزة + الهدف)
**المصدر:** CDC BRFSS 2015

### 1.1 اتجاه التسمية ✅

فُحص بتسعة مؤشرات سريرية مستقلة. `Diabetes_binary = 1` يعني مريضاً في كلٍّ منها:

```
HighBP                 label0  0.374  ->  label1  0.753
HighChol               label0  0.381  ->  label1  0.670
BMI                    label0 27.770  ->  label1 31.944
GenHlth                label0  2.383  ->  label1  3.291    (1=ممتاز .. 5=سيء)
Age                    label0  7.789  ->  label1  9.379
DiffWalk               label0  0.134  ->  label1  0.371
HeartDiseaseorAttack   label0  0.073  ->  label1  0.223
Stroke                 label0  0.032  ->  label1  0.092
PhysActivity           label0  0.776  ->  label1  0.631    (ينخفض — صحيح)
```

**لا انقلاب.** مصدر واحد، فلا يوجد احتمال لتضارب اصطلاحات كما حدث في القلب.

### 1.2 القيم المفقودة ✅

صفر قيم مفقودة عبر كل الأعمدة الـ22.

### 1.3 التكرارات ⚠️

```
صفوف مكررة تماماً في الملف         : 1,635  (2.31%)
صفوف اختبار لها نظير في التدريب   :   564 / 14,139  (3.99%)

ROC-AUC على كل صفوف الاختبار      : 0.8302
ROC-AUC بعد استبعاد المتطابقة     : 0.8253   (n = 13,575)
```

**الأثر 0.005** — ضمن الضجيج.

**للدفاع:** كل الميزات الـ21 ثنائية أو رتبية خشنة (`HighBP` ثنائية، `GenHlth` من 1 إلى 5، `Age` فئات عمرية). تطابق مريضين في 21 خانة من هذا النوع **متوقع إحصائياً** ولا يعني بالضرورة تكراراً حقيقياً لنفس الشخص. لكن هذه الصفوف تُحفظ لا تُتنبأ، فالرقمان يُذكران معاً.

---

## 2. العتبة — خللان منفصلان

### 2.1 استُخرجت من مجموعة الاختبار ❌

**الموقع:** `models/train_diabetes_ensemble.py:933-934`

```python
threshold_result = find_optimal_clinical_threshold(
    y_true=y_test,          # ← مجموعة الاختبار
```

العتبة `0.275` المشحونة في `configs/diabetes.yaml:87` اختيرت بالنظر لنفس البيانات التي تُبلَّغ عليها النتائج. منهجياً هذا اختيار ملوَّث.

**القياس على مجموعة اختبار محجوبة:**

```
0.2750  المشحونة (من y_test)          حساسية 93.32%  نوعية 50.74%  FN 472  FP 3483
0.2907  إعادة اشتقاق من y_test         حساسية 92.69%  نوعية 52.60%  FN 517  FP 3351
0.3203  من out-of-fold تدريب  ← الصحيح حساسية 91.33%  نوعية 55.15%  FN 613  FP 3171

الكلفة (2×FN + 1×FP) عند المشحونة : 4427
الكلفة عند العتبة الصحيحة          : 4397
```

**الفرق 0.7%.**

**تحذير في الصياغة:** لا تصف هذا بأنه «تسريب أفسد النتائج». حجم التدريب (56,553 صفاً) يبتلع تحيّز الاختيار. الصياغة الصحيحة: *خلل منهجي أُصلح، وقِيس أثره فكان 0.7% في الكلفة و1.5 نقطة في الحساسية.*

**ملاحظة:** نفس الخلل موجود في الوحدتين — في القلب كانت العتبة المدّعاة `0.420` غير موجودة في الكود أصلاً؛ هنا موجودة لكن مستخرجة من المكان الخطأ.

### 2.2 المعايرة على نسبة انتشار خاطئة ❌ — المشكلة الأكبر

الملف `diabetes_binary_5050split` **موازن صناعياً 50/50**. الانتشار الحقيقي في BRFSS حوالي **14%**.

**الأثر، بتثبيت النموذج والعتبة وتغيير الانتشار فقط:**

| الانتشار | يُصنّفون موجب | حساسية | نوعية | **PPV** | لكل 1000 مفحوص |
|---|---|---|---|---|---|
| 50% | 68.1% | 91.3% | 55.1% | 67.1% | 680 محوَّلاً، 224 بلا داعٍ |
| 30% | 58.7% | 90.9% | 55.1% | 46.5% | 586 محوَّلاً، 313 بلا داعٍ |
| 20% | 54.2% | 91.6% | 55.1% | 33.8% | 541 محوَّلاً، 358 بلا داعٍ |
| **14%** ← الواقع | **51.3%** | 90.6% | 55.1% | **24.7%** | **512 محوَّلاً، 385 بلا داعٍ** |
| 10% | 49.4% | 90.8% | 55.1% | 18.4% | 494 محوَّلاً، 403 بلا داعٍ |

الحساسية والنوعية ثابتة — هما مستقلتان عن الانتشار. **الـPPV ينهار من 67% إلى 25%.**

**عملياً:** عند الانتشار الحقيقي يحوّل النظام نصف كل من يفحصهم، وثلاثة من كل أربعة محوَّلين ليسوا مرضى.

**تحقق نظري:** المعادلة تتنبأ بالرقم قبل التجربة.

```
PPV = (Se·π) / (Se·π + (1−Sp)(1−π))
    = (0.906 × 0.14) / (0.906 × 0.14 + 0.449 × 0.86)
    = 0.1268 / 0.5129
    = 24.7%    ← يطابق القياس التجريبي بالضبط
```

فالظاهرة خاصية رياضية، لا مصادفة في هذه البيانات.

**ارتباط بتوثيق سابق:** `plans/omnidiag_problem_analysis_report.md §6.4` يذكر «إجهاد الإنذارات» بنوعية 54.6%. الرقم الحقيقي أسوأ، وله الآن سبب محدد وقابل للقياس.

---

## 3. تجربة الانتشار — هل الموازنة تضرّ؟

**التصميم:** حجم تدريب مثبَّت (32,879 صفاً لكل ذراع)، 5 بذور، مجموعة اختبار واحدة لا تتغير عند انتشار 14%. النسبة هي المتغير الوحيد.

### 3.1 القدرة على الترتيب — لا تتأثر

```
نسبة التدريب      ROC-AUC
14%              0.8248 ± 0.0048
25%              0.8260 ± 0.0050
40%              0.8283 ± 0.0061
50%              0.8284 ± 0.0047
```

المدى الكامل `0.0036`، والضجيج بين البذور `±0.005`. **الفرق أصغر من ضجيج التجربة.**

### 3.2 المقاييس المعتمدة على المقياس — تنهار

```
نسبة التدريب     الدقة      Brier     متوسط التنبؤ
14%             84.88%     0.0984      0.1365
25%             78.71%     0.1085      0.2175
40%             69.89%     0.1426      0.3131
50%             64.01%     0.1760      0.3763
                                       (الحقيقة 0.1400)
```

**الدقة تتغير 21 نقطة** لنفس القدرة. النموذج المدرَّب على 50% يبالغ في تقدير الخطر بمعامل 2.7.

> **تصحيح مهم:** الدقة **ليست** مستقلة عن الانتشار.
> `Accuracy = Se·π + Sp·(1−π)` — فيها π صراحةً.
> المستقل عن الانتشار: ROC-AUC، الحساسية، النوعية، نسبة الأرجحية.
> المعتمد عليه: الدقة، PPV، NPV، Brier، العتبة المثلى.

### 3.3 تصحيح بايز القبلي — يصلحها بلا إعادة تدريب

```
                 Brier قبل  →  بعد      الحساسية   النوعية
50%              0.1760  →  0.0978      لم تتغير   لم تتغير
40%              0.1426  →  0.0978      لم تتغير   لم تتغير
25%              0.1085  →  0.0983      لم تتغير   لم تتغير
14%              0.0984  →  0.0984      (لا تصحيح مطلوب)
```

كل الأذرع تتقارب إلى `Brier ≈ 0.098` — وهي قيمة النموذج المدرَّب على البيانات الحقيقية أصلاً.

**والحساسية والنوعية لم تتغيرا رقماً واحداً**، مما يؤكد أن التصحيح إعادة تسمية رتيبة: يغيّر الرقم المعروض، لا من يُصنَّف موجباً.

**المعادلة:**

```python
R = (π_deploy / (1 - π_deploy)) / (π_train / (1 - π_train))
p_real = (R * p_model) / (1 - p_model + R * p_model)
```

بقيم النظام: `π_train = 0.50`، `π_deploy = 0.14` → `R = 0.1628`.
العتبة `0.3203` على المقياس الموازن تقابل `0.0712` على المقياس المصحَّح.

### 3.4 `scale_pos_weight` ليس بديلاً ❌

```
scale_pos_weight @ natural    ROC-AUC 0.8193    Brier 0.1613    متوسط التنبؤ 0.3424
```

أقل AUC وأسوأ معايرة من أذرع إعادة العينة. يفسد المعايرة بنفس الطريقة، وتصحيح بايز عليه أعقد لأن النسبة الفعالة ليست نسبة البيانات.

**هذا يلغي توصية سابقة** كانت تقترح استخدامه بديلاً عن الموازنة اليدوية.

---

## 4. قيود هذا التدقيق

| القيد | الأثر |
|---|---|
| تجربة الانتشار استخدمت الملف الموازن كاحتياطي | مجموعات التدريب بُنيت بحذف موجبات؛ مجموعة الـ14% **تركيبية**. الاتجاهات صحيحة، الأرقام المطلقة أضعف |
| لم يُفحص خط معالجة السكري مباشرة | `run_diabetes_preprocessing()` لم يُراجع؛ الاعتماد على تدقيق سابق قال «فرق 0.000» |
| لم تُفحص بنية الـStacking | لم يُتحقق أن الـensemble يتفوق على XGBoost مفرد |
| لم تُفحص هندسة ميزات السكري | الميزات الخمس المشتقة لم تخضع لاختبار ablation كما في القلب |
| لا يوجد تحقق خارجي | لا مجموعة مستقلة تقابل ما فعلته كوهورت طهران للقلب |

---

## 5. ما يجب عمله الآن — مرتّباً

### أولوية 1 — تصحيح الانتشار (~30 دقيقة)

أهم إصلاح، وأرخصه.

```python
# في EnsembleModelLoader.predict()، قبل إرجاع confidence
R = 0.1628                                    # (0.14/0.86) / (0.50/0.50)
p_real = (R * p_model) / (1 - p_model + R * p_model)
```

وتحديث `configs/diabetes.yaml`:
```yaml
inference_threshold: 0.0712      # على المقياس المصحَّح
```

والفرونت يقرأ العتبة من استجابة الـAPI أصلاً (`frontend/src/constants/thresholds.js:41`)، فينتقل تلقائياً.

**لا إعادة تدريب.** النموذج كما هو.

### أولوية 2 — إعادة اشتقاق العتبة من out-of-fold (~20 دقيقة)

تعديل `train_diabetes_ensemble.py:933` من `y_true=y_test` إلى احتمالات out-of-fold من مجموعة التدريب. يرفع النوعية 4.4 نقطة ويزيل الخلل المنهجي.

يُنفَّذ **قبل** الأولوية 1 ليُصحَّح الرقم الصحيح.

### أولوية 3 — تنزيل الملف الكامل وإعادة التشغيل (~ساعة)

```
diabetes_binary_health_indicators_BRFSS2015.csv    253,680 صفاً، انتشار حقيقي
```

نفس الأعمدة الـ21، من نفس صفحة Kaggle. يزيل قيد §4 الأول، ويكبّر البيانات 3.6 أضعاف.
`prevalence_experiment.py` يلتقطه تلقائياً ويشيل تحذير الاحتياطي.

### أولوية 4 — ablation لميزات السكري (~ساعة)

نفس ما عُمل للقلب. الميزات الخمس المشتقة لم تُختبر قط، وقد تكون منها ما يضرّ.

### أولوية 5 — التحقق من الـStacking (~ساعة)

هل يتفوق على XGBoost مفرد فعلاً؟ سؤال متوقع من اللجنة، ولا جواب مقاس له حالياً.

### أولوية 6 — تحقق خارجي على NHANES (يوم+)

فحوصات سريرية حقيقية لا استبيان هاتفي. يعطي وحدة السكري ما تعطيه طهران لوحدة القلب.
**خارج نطاق 17 يوم** ما لم تُنجَز الأولويات الأعلى.

---

## 6. الصياغة المقترحة للجنة

> درّبنا على مجموعة موازنة 50/50 لمنع تحيّز التعلّم نحو الصنف الأغلب. لكن الموازنة تفسد معايرة الاحتمالات ولا تفسد الترتيب — قِسنا ذلك بحجم تدريب مثبَّت وخمس بذور: تحريك نسبة التدريب من 14% إلى 50% غيّر ROC-AUC بمقدار 0.0036 فقط (ضمن ±0.005 من ضجيج التجربة)، بينما غيّر الدقة 21 نقطة والـBrier من 0.098 إلى 0.176.
>
> لذلك نطبّق تصحيح الانتشار القبلي بمبرهنة بايز قبل عرض الاحتمال للطبيب، ونشتق العتبة على المقياس المصحَّح. التصحيح يعيد Brier إلى 0.098 بلا إعادة تدريب، والحساسية والنوعية تبقيان دون تغيير — وهو ما يثبت أنه تحويل وحدات لا قرار جديد.

---

## 7. المقارنة مع وحدة القلب

| | القلب | السكري |
|---|---|---|
| اتجاه التسمية | ❌ مكسور (مصدران متعاكسان) → أُصلح | ✅ سليم |
| ميزات ملفَّقة | ❌ نصف البيانات (`Oldpeak=0.0`) | ✅ لا يوجد |
| تسريب المعالجة | قِيس: 0.001 AUC | لم يُفحص مباشرة |
| منهجية العتبة | ❌ لم تُطبَّق أصلاً (argmax) | ❌ من test — أثر 0.7% |
| **معايرة الانتشار** | غير منطبق | ❌ **PPV 67% → 25%** |
| تحقق خارجي | ✅ طهران، 303 مرضى | ❌ لا يوجد |
| ROC-AUC | 0.8081 (LOSO) | 0.8302 (داخلي) |

**رسالة رئيسية:** الوحدتان فيهما خلل، لكن من **نوعين مختلفين تماماً**. القلب كانت بياناته مكسورة؛ السكري بياناته نظيفة لكنها لا تشبه الواقع. الخلط بينهما في العرض يُضعف الإجابة.

---

*انتهى. كل رقم في هذا التقرير قابل لإعادة الإنتاج بتشغيل `audit_diabetes.py` و`prevalence_experiment.py`.*
