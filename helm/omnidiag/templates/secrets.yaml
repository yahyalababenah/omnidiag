# ⚠️  Template only — do NOT put real values here.
# Create the secret manually:
#   kubectl create secret generic omnidiag-secrets \
#     --from-literal=database-url="postgresql+asyncpg://..." \
#     --from-literal=redis-url="redis://omnidiag-redis:6379" \
#     --from-literal=jwt-secret="..." \
#     --from-literal=postgres-user="omnidiag" \
#     --from-literal=postgres-password="..." \
#     -n omnidiag
#
# This file is intentionally left without actual secret data.
