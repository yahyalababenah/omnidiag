# ⚠️  DO NOT commit real values — this is a template only.
# Use: kubectl create secret generic omnidiag-secrets --from-env-file=.env
apiVersion: v1
kind: Secret
metadata:
  name: omnidiag-secrets
  namespace: omnidiag
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://omnidiag:CHANGE_ME@omnidiag-postgres:5432/omnidiag_db"
  redis-url: "redis://omnidiag-redis:6379"
  jwt-secret: "CHANGE_ME_32_CHAR_SECRET"
  postgres-user: "omnidiag"
  postgres-password: "CHANGE_ME"
