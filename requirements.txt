# =============================================================================
# OmniDiag — Python Dependencies
# =============================================================================
# All libraries use minimum versions (>=) to balance reproducibility with
# receiving security updates. The versions specified are the minimum required
# for the APIs used by this project.
# =============================================================================

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# ── Data processing ────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Machine learning ───────────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# ── SHAP explainability ────────────────────────────────────────────────────
shap>=0.42.0

# ── Configuration ──────────────────────────────────────────────────────────
pyyaml>=6.0
pydantic>=2.0.0

# ── Counterfactual explanations ────────────────────────────────────────────
# NOTE: dice-ml (v0.12) is incompatible with pandas >= 3.0 (LossySetitemError).
#       Replaced with a custom DiCE-inspired implementation in
#       backend/counterfactual_generator.py using numpy random sampling +
#       diversity selection — no external dependencies beyond numpy/pandas.
# dice-ml>=0.12
# ── Visualization (for training/reference scripts only) ────────────────────
# matplotlib>=3.7.0
